<table data-uri="{{ uri }}" class="table data-table" role="grid">
    <thead>
    <tr>
        {% for column in table.getColumns() %}
            {{ datatables_render_column(column) }}
        {% endfor %}
    </tr>
    </thead>
    <tbody>
        <!--- data be here --->
    </tbody>
</table>
