<table data-uri="{{ uri }}" data-alias="{{ table.alias }}" class="table datatable" role="grid">
    <thead>
        {% include 'DatatablesBundle:Datatables:default_table_header.html.twig' %}
    </thead>
    <tbody>
        <!--- data be here --->
    </tbody>
</table>
